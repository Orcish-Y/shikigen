[package]
name = "shikigen-desktop"
version = "0.1.0"
description = "Shikigen desktop application"
authors = []
edition = "2021"

[lib]
name = "shikigen_desktop_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
